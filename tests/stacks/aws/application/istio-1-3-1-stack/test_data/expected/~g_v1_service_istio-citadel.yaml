apiVersion: v1
kind: Service
metadata:
  labels:
    app: istio-install
    istio: citadel
    kustomize.component: istio-install
  name: istio-citadel
  namespace: istio-system
spec:
  ports:
  - name: grpc-citadel
    port: 8060
    protocol: TCP
    targetPort: 8060
  - name: http-monitoring
    port: 15014
  selector:
    app: istio-install
    istio: citadel
    kustomize.component: istio-install
